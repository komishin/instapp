.container
  %h2 新規投稿
  = form_with model: @post, local: true do |f|
    .field
      = f.label :images, "画像を選択（複数可）"
      = f.file_field :images, multiple: true, direct_upload: true, class: 'form-control'

    .field
      = f.label :caption, "キャプション"
      = f.text_area :caption, placeholder: "What's on your mind ?", class: 'form-control'

    .actions
      = f.submit "投稿する", class: 'btn-primary'